name: Build and Test Muduo

on:
  push:
    branches:
      - master
  pull_request:
    branches:
      - master

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Install build dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            g++ \
            cmake \
            make \
            vim \
            wget \
            libboost-dev \
            git \
            protobuf-compiler libunwind-dev libprotoc-dev \
            libgoogle-perftools-dev \
            libc-ares-dev libcurl4-openssl-dev

      - name: Build Muduo
        run: |
          echo "Building Muduo..."
          # Replace this with actual build commands once source code is ready
          echo "Mock: Build completed successfully."

      - name: Set execute permission for build script
        run: |
          chmod +x build.sh

      - name: Build Milestone1 Codes
        run: |
          ./build.sh build    

      - name: Run Milestone1 Tests
        run: |
          echo "Running tests..."
          ./build.sh run

      - name: Clean Milestone1 Workdir
        run: |
          ./build.sh clean     
